#include <stdio.h>

int main()
{
  int binary1, binary2;

  binary1 = 0100;
  binary2 = 0111;

  // Q. what is sum of binary1 and binary2? 
  // Hint: check base 2 (binary), 8 (octal), 16 (hexadecimal) notation. 

  printf("%d \n", binary1 + binary2);

  return 0;
}
